import { LoadingIndicator, Container } from '../loading/styles';

const Loading = () => {
    return (
        <Container>
            <LoadingIndicator/>
        </Container>
    )
}

export default Loading;